<VirtualHost *:80>
	#Alias don't works just in here for a unkown reason
	Alias /frontend /var/www/html/git/XStore/utilizacao

	ServerName 	www.xstore.com.br
	ServerAlias 	xstore.com.br

	ServerAdmin webmaster@localhost
	DocumentRoot /var/www/html/git/XStore/utilizacao

	ErrorLog ${APACHE_LOG_DIR}/error_xstore.log
	CustomLog ${APACHE_LOG_DIR}/access_xstore.log combined
</VirtualHost>
